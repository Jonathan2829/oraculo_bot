# fly.toml - oraculo-bot (worker)
app = "oraculo-bot"
primary_region = "ams"

[build]
  dockerfile = "Dockerfile"

[processes]
  worker = "python -m oraculo_bot.main"

[[vm]]
  memory = "1gb"
  cpus = 1

[deploy]
  strategy = "immediate"
